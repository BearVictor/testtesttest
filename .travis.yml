language: cpp
os: linux

before_install:
    - sudo apt update
    - sudo apt install python3.6
    
    - python --version
    - python3 --version
    - python3.6 --version
    - pip list

install:
  - pip install -r requirements.txt
script:
  — python pytest
