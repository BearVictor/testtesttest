language: cpp
os: linux

conan-linux: &conan-linux
    os: linux
    dist: xenial
    language: python
    python: "3.6"
# Install necessary packages
before_install:
    - python --version
    - pip list

install:
  - pip install -r requirements.txt
script:
  — python pytest
